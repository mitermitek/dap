import { DisplayTabNamePipe } from './display-tab-name-pipe';

describe('DisplayTabNamePipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayTabNamePipe();
    expect(pipe).toBeTruthy();
  });
});
